# ── Core Web Framework ────────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.5
Flask-Limiter==4.1.1
Werkzeug==3.1.8
flasgger==0.9.7.1
waitress==3.0.2

# ── Data / ML ────────────────────────────────────────────────────────
numpy==2.4.2
pandas==2.3.3
pyarrow==23.0.1
scikit-learn==1.8.0
joblib==1.5.3
imbalanced-learn==0.14.2
nltk==3.9.4

# ── Deep Learning ────────────────────────────────────────────────────
torch==2.12.1
transformers==5.12.1
sentence-transformers==5.6.0
accelerate==1.14.0
safetensors==0.8.0
tokenizers==0.22.2

# ── LLM Agent ────────────────────────────────────────────────────────
openai==2.30.0

# ── Scraping / Parsing ───────────────────────────────────────────────
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1

# ── Monitoring / Ops ─────────────────────────────────────────────────
prometheus_client==0.25.0
sentry-sdk[flask]==2.63.0
mlflow==3.10.1

# ── Caching ──────────────────────────────────────────────────────────
# Optional shared backend for the response cache; the in-process LRU tier
# works without a running Redis server (client import only, lazy connect).
redis==5.2.1

# ── Dimensionality Reduction ─────────────────────────────────────────
umap-learn==0.5.11
# hdbscan 0.8.43+ is the first release publishing cp314 wheels (Python 3.14);
# 0.8.41 has none, which would force a fragile from-source build on 3.14.
hdbscan==0.8.44

# ── Utilities ────────────────────────────────────────────────────────
dill==0.4.1
pydantic==2.13.4
python-dotenv==1.2.2
PyYAML==6.0.3
